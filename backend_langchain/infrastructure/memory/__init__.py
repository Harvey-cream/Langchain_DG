"""
Agent memory：

- 会话压缩：memory.py / memory_trim / token_budget / memory_summary / memory_persist
- 长期记忆写图：infrastructure.memory.agent（trigger→extract→apply）
- 读侧/调度：long_term_agent + long_term_store + long_term_trigger
"""
