# 本地建库 / 评测（Scripts/build_rag_knowledge.py、chunk.py、test_rag_eval.py）
# 在 requirements.txt 基础上追加；生产容器不必安装。

-r requirements.txt

langchain-community>=0.4.1,<0.5
langchain-text-splitters>=1.1.1,<2.0

# 本地 HuggingFace 嵌入（APP_ENV=debug）还需 CPU 版 PyTorch，例如：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# 并设置 HF_EMBEDDING_MODEL、HF 缓存目录等。
