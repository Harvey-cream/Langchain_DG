# 仅本地开发：DJANGO_ENV 非 production/pro/prod 时用 HuggingFace 嵌入。
# 建议顺序：
#   pip install -r requirements.txt
#   pip install "torch==2.2.2+cpu" --index-url https://download.pytorch.org/whl/cpu   # 可选，避免 CUDA 大包
#   pip install -r requirements-dev.txt
#
# 若报错：cannot import name 'logging' from 'huggingface_hub'
#   多为 Roaming 里旧版/损坏的 huggingface_hub，执行：
#   pip install -U "huggingface_hub>=0.26.0" "transformers>=4.46.0" "sentence-transformers>=5.3.0,<6.0"

huggingface_hub>=0.26.0,<1.0
transformers>=4.46.0,<5.0
sentence-transformers>=5.3.0,<6.0
