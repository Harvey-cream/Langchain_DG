# Agent 架构
## 总览（纯文本 · 任意编辑器可见）

```
                    ┌──────── 用户 ────────┐
                    │                      │
         ┌──────────┴──────────┐  ┌────────┴──────────┐
         │  企业知识库 Agent    │  │   面试 Agent 线    │
         │  上传→清洗→RAG→问答  │  │  Workflow 路由     │
         └──────────┬──────────┘  │  ├ JD Agent        │
                    │             │  ├ 模拟面试 Agent   │
                    │             │  ├ 评估 Agent       │
                    │             │  └ …可扩展          │
                    │             └────────┬──────────┘
                    └──────────┬───────────┘
                               ▼
                    ┌────── 共用底座 ──────┐
                    │ skill_recall→agent   │
                    │ RAG · MCP · PDF      │
                    │ 会话记忆 SQLite      │
                    └──────────┬───────────┘
                               │ 流式结束后异步
                               ▼
                    ┌── Memory Agent ──────┐
                    │ 用户画像 UserProfile │
                    └──────────────────────┘
```

- **Skill**：各 Agent / 子 Agent 独立一套，`skill_recall` 路由  
- **Memory Agent**：主回复完成后异步执行，不挡响应

## Agent 视图

```
┌─────────────────────────────────────────────────────────────┐
│                        Agent 层                              │            
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────────────┐      ┌─────────────────────┐      │
│   │  企业知识库 Agent 线 │      │    面试 Agent 线     │      │
│   └──────────┬──────────┘      └──────────┬──────────┘      │
│              │                            │                 │
│   ┌──────────┼──────────┐    ┌─────────────┼─────────────┐   │
│   ▼          ▼          ▼    ▼             ▼             ▼   │
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌─────────┐ ┌───────────┐ ┌─────────┐
│ │文档摘要 │ │合规速查 │ │企业问答 │ │JD Agent │ │模拟面试    │ │评估     │
│ │Agent   │ │Agent   │ │Agent   │ │         │ │Agent      │ │Agent    │
│ └────────┘ └────────┘ └────────┘ └─────────┘ └───────────┘ └─────────┘
│              … 入库 Pipeline（非 Agent）…    … 可扩展 …       │
│                                                              │
│   ┌─────────────────────┐                                    │
│   │   Memory Agent      │  ← 异步后置，更新用户画像           │
│   └─────────────────────┘                                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Agent 说明

### 共用技术

| 技术 | 作用 |
|------|------|
| **LangGraph** | `skill_recall → agent ↔ tools`，子 Agent 共用图工厂 |
| **Skill 路由** | 向量匹配意图 + 约束输出结构（每 Agent 独立 Skill 集） |
| **RAG** | Chroma；`corpus` / `domain` 隔离企业库与面试库 |
| **会话记忆** | SQLite checkpoint；超阈值压缩（摘要 + 保留近几轮） |
| **SSE** | 流式对话；附件当轮临时注入，不落库 |

---

### 企业知识库线

**入库 Pipeline（非 Agent）**  
`上传 → 清洗切块 → 向量化 → 写入企业 corpus`  
后台任务，不走 LLM 对话；为下方 Agent 提供知识源。

| Agent | 职责 | RAG / 工具 |
|-------|------|------------|
| **文档摘要 Agent** | 单篇/多篇导读、结构化摘要 | 企业 corpus |
| **合规速查 Agent** | 制度/政策问答；输出条款 + 出处 | 企业 corpus；输出格式固定 |
| **企业问答 Agent** | 日常企业知识问答（核心） | 企业 corpus；可选 MCP / PDF |

```
入库 Pipeline ──→ 企业 RAG
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
    文档摘要    合规速查    企业问答
```

---

### 面试 Agent 线

Workflow 按意图路由子 Agent，可串联：`JD → 模拟 → 评估`。

| Agent | 职责 | RAG / 工具 |
|-------|------|------------|
| **JD Agent** | 生成/优化岗位描述 | 可选模板库；结构化输出 |
| **模拟面试 Agent** | 出题、追问、答题辅导 | `interview` corpus；按赛道过滤 |
| **评估 Agent** | 作答评分、维度分析、复盘 | 结合当轮对话；可导出 PDF |

---

### Memory Agent（异步）

| 项 | 说明 |
|----|------|
| **时机** | 主回复流式结束、MySQL 落库之后 |
| **职责** | 决策是否更新 `UserProfile`（目标岗、技术栈、偏好等） |
| **原则** | 不进主链路，不增加首 token 延迟 |

```
主 Agent 流式回复 → done →（异步）Memory Agent → UserProfile
```

